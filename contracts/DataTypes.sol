// SPDX-License-Identifier: MIT
pragma solidity 0.8.14;

enum AccessoryType {
    Skin,
    Body,
    EyeWear,
    HeadWear,
    Props
}

enum BoxType {
    Virtual,
    Bronze,
    Silver,
    Gold,
    Platinum,
    Diamond
}

enum ExpressionType {
    Normal,
    ExpressionA,
    ExpressionB
}

enum FinishType {
    Normal,
    Holo
}

enum BackgroundLine {
    Dots,
    Flash,
    Hex,
    Rain,
    Spotlight,
    Mozart,
    Affinity,
    Arena,
    Token,
    Encounter
}
